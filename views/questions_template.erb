<!DOCTYPE html>

<html>
  <head>
    <title> Vocational Test </title>
    <link rel="stylesheet" type="text/css" href='/stylesheet.css'/>
  </head>

  <body>
    <nav class='mainNav'>
      <div height='100px'>
        <a href='/'>
          <img id='logo' src='/Flying_Hellfish_Logo.png'/>
        </a>
        <h1 id='mainTitle'>
          Taking the test
        </h1>
        <div class='topNav'>
          <a href='/'>Home</a>
          <a href='/careers'>Careers</a>
          <a href='/surveys'>Surveys</a>
        </div>
      </div>  
    </nav>
    <br>
    <div class='quesTitle'>
      <a class='qTitle'> <%= @survey_id %>) </a> 
      <hr>
      <a class='qTitle'> <%= @question.number %>) <%= @question.name %> </a> 
      <hr>
      <a class='qDesc'> <%= @question.description %> </a> <br>
    </div>
    <br>
    <div>
      <form action='/responses/<%= @survey_id.to_s %>' method='POST'>
		<% @question.choices.each do |choice| %>
			<label class="container"><%= choice.text %>
				<input type="radio" name="choice_id" value=<%= choice.id %>>
				<span class="checkmark"></span>
			</label>
		<% end %>
        <button class='button' type='submit' name='question_id' value='<%= @question.id %>' > Next </button> 
      </form>
    </div>
  </body>
</html>
